# Rust build output
/target/

# Dependency lock snapshots generated by cargo
# (keep Cargo.lock for binaries, ignore for libraries — this is a binary workspace)
# Cargo.lock

# sqlx offline query cache
.sqlx/

# Environment — never commit secrets
.env
.env.local
.env.*.local

# Editor and OS artifacts
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Compiled test fixtures / fuzz corpus
fuzz/target/
